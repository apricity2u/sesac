package org.example.etc;

public enum Day {
    // 요일을 모은다고 했을 때, 작성 형태가 모두 다르면 안됨.
    // 작성 형태에 대한 일종의 약속

    // 각각이 인스턴스다. new 로 안만들어줘도 됨
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY;
}
